

   var leftXml = selectedItems.Count > 0
        ? selectedItems[0].Response
        : string.Empty;

    LeftSelectedItemTitle = selectedItems.Count > 0
        ? selectedItems[0].Description?.ToString() ?? string.Empty
        : string.Empty;

    var rightXml = selectedItems.Count > 1
        ? selectedItems[1].Response
        : string.Empty;

    RightSelectedItemTitle = selectedItems.Count > 1
        ? selectedItems[1].Description?.ToString() ?? string.Empty
        : string.Empty;

