request = memberSearchResult.Request;
response = memberSearchResult.RootResponse;

// Existing logic to determine HostResponse

await HostResponseLogger.SaveAsync(
    selectedEnvironment,
    selectedSearch,
    request,
    response,
    HostResponse);
