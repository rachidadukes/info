var prompt = new SelectionPrompt<AccountNumbersModel>()
    .Title("Account Number Selection")
    .PageSize(10)
    .UseConverter(x => $"{x.AccountNumber ?? ""} - {x.AccountType ?? ""}")
    .MoreChoicesText("[grey](Move up and down to reveal more items)[/]")
    .AddChoices(listResult);
