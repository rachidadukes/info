using System.IO;
using System.Text;
using System.Windows;
using System.Windows.Documents;
using System.Windows.Media;
using System.Xml;

namespace WpfApiTester.Helpers
{
    public sealed class XmlRichTextFormatter
    {
        private static readonly HashSet<string> HighlightedElementNames =
            new(StringComparer.OrdinalIgnoreCase)
            {
                "CustPermId",
                "TranCode",
                "AcctId",
                "AcctType",
                "FirstName",
                "LastName"
            };

        private static readonly Brush HighlightBrush =
            new SolidColorBrush(Color.FromRgb(0, 0, 180));

        public FlowDocument FormatXml(string xml)
        {
            var document = new FlowDocument
            {
                PagePadding = new Thickness(5),
                ColumnWidth = 800
            };

            var paragraph = new Paragraph
            {
                Margin = new Thickness(0),
                LineHeight = 1
            };

            document.Blocks.Add(paragraph);

            if (string.IsNullOrWhiteSpace(xml))
                return document;

            try
            {
                using var stringReader = new StringReader(xml);

                var settings = new XmlReaderSettings
                {
                    IgnoreWhitespace = true,
                    IgnoreComments = false,
                    IgnoreProcessingInstructions = false
                };

                using var reader = XmlReader.Create(stringReader, settings);

                WriteXml(reader, paragraph);
            }
            catch (XmlException)
            {
                AddLine(paragraph, 0, xml);
            }

            return document;
        }

        private static void WriteXml(XmlReader reader, Paragraph paragraph)
        {
            var depth = 0;

            while (reader.Read())
            {
                switch (reader.NodeType)
                {
                    case XmlNodeType.Element:
                        WriteStartElement(reader, paragraph, depth);

                        if (!reader.IsEmptyElement)
                            depth++;

                        break;

                    case XmlNodeType.EndElement:
                        depth--;
                        AddLine(paragraph, depth, $"</{reader.Name}>");
                        break;

                    case XmlNodeType.Text:
                    case XmlNodeType.CDATA:
                        AddLine(paragraph, depth, reader.Value);
                        break;

                    case XmlNodeType.Comment:
                        AddLine(paragraph, depth, $"<!--{reader.Value}-->");
                        break;

                    case XmlNodeType.ProcessingInstruction:
                        AddLine(paragraph, depth, $"<?{reader.Name} {reader.Value}?>");
                        break;
                }
            }
        }

        private static void WriteStartElement(
            XmlReader reader,
            Paragraph paragraph,
            int depth)
        {
            var elementName = reader.Name;
            var attributes = GetAttributes(reader);

            if (reader.IsEmptyElement)
            {
                AddXmlLine(paragraph, depth, $"<{elementName}{attributes} />", elementName);
                return;
            }

            AddXmlLine(paragraph, depth, $"<{elementName}{attributes}>", elementName);
        }

        private static string GetAttributes(XmlReader reader)
        {
            if (!reader.HasAttributes)
                return string.Empty;

            var builder = new StringBuilder();

            while (reader.MoveToNextAttribute())
            {
                builder.Append(' ');
                builder.Append(reader.Name);
                builder.Append("=\"");
                builder.Append(reader.Value);
                builder.Append('"');
            }

            reader.MoveToElement();

            return builder.ToString();
        }

        private static void AddXmlLine(
            Paragraph paragraph,
            int depth,
            string text,
            string elementName)
        {
            AddLine(
                paragraph,
                depth,
                text,
                HighlightedElementNames.Contains(elementName));
        }

        private static void AddLine(
            Paragraph paragraph,
            int depth,
            string text,
            bool highlight = false)
        {
            var indent = new string(' ', Math.Max(depth, 0) * 4);

            var run = new Run(indent + text);

            if (highlight)
            {
                run.Foreground = HighlightBrush;
                run.FontWeight = FontWeights.Bold;
            }

            paragraph.Inlines.Add(run);
            paragraph.Inlines.Add(new LineBreak());
        }
    }
}
