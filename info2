if (selectedType == TypeValues.Type.MemberSearch)
{
    string selectedMemberSearchType = GetSelectedMemberSearchType();

    requestsQuery = requestsQuery
        .Where(r => r.MemberSearchType == selectedMemberSearchType);
}
