
CREATE VIEW vw_RequestTypeTranCodes AS
SELECT DISTINCT
       TRIM(RequestType) AS RequestType,
       TRIM(TranCode) AS TranCode
FROM LogDetails
WHERE RequestType IS NOT NULL
  AND TranCode IS NOT NULL
  AND TRIM(RequestType) <> ''
  AND TRIM(TranCode) <> '';
