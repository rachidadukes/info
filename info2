

var q = db.VerifiedAccounts.AsNoTracking()
    .Where(r => r.AccountNumber != null);

if (!string.IsNullOrWhiteSpace(startsWith))
{
    q = q.Where(r => r.AccountNumber!.StartsWith(startsWith));
}

if (!string.IsNullOrWhiteSpace(environment))
{
    q = q.Where(r => r.Environment == environment);
}

if (verified.HasValue)
{
    q = q.Where(r => r.Verified == verified.Value);
}


=====================
Install-Package System.Linq.Dynamic.Core
==================

ReferenceDataService.cs

public async Task<PagedResult<VerifiedAccountsModel>> GetVerifiedAccountNumbersAsync(
    string? startsWith = null,
    int take = 50,
    int skip = 0,
    string? environment = null,
    bool? verified = null,
    string? filter = null,
    string? orderBy = null,
    CancellationToken ct = default)

=============================================
ReferenceDataController.cs


[HttpGet("verifiedaccounts")]
[EndpointSummary("All Verified Account Numbers")]
[ProducesResponseType(typeof(PagedResult<VerifiedAccountsModel>), StatusCodes.Status200OK)]
public async Task<ActionResult<PagedResult<VerifiedAccountsModel>>> GetVerifiedAccounts(
    [FromQuery] string? startsWith,
    [FromQuery] int take = 50,
    [FromQuery] int skip = 0,
    [FromQuery] string? environment = null,
    [FromQuery] bool? verified = null,
    [FromQuery] string? filter = null,
    [FromQuery] string? orderBy = null,
    CancellationToken ct = default)
{
    var result = await svc.GetVerifiedAccountNumbersAsync(
        startsWith,
        take,
        skip,
        environment,
        verified,
        filter,
        orderBy,
        ct);

    return Ok(result);
}
