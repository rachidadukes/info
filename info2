using System.Xml.Linq;

private string ExtractTransactionStatus(string response)
{
    try
    {
        var doc = XDocument.Parse(response);

        var status = doc.Descendants("transactionstatus").FirstOrDefault();

        return status?.Value ?? "Unknown";
    }
    catch
    {
        return "Unknown";
    }
}
