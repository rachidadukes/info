  var entry = new ApiCallHistory(DateTime.Now, description, requestBody, responseBody, null, TransactionType.MemberProfile, !string.IsNullOrWhiteSpace(responseBody), requestType, selectedUrl.Environment);
  await SaveResponseAsync(entry);
