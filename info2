private void cmbTagNames_SelectionChanged(object sender, SelectionChangedEventArgs e)
{
    if (cmbTagNames.SelectedItem is TagNameListModel selectedItem)
    {
        selectedTagName = selectedItem.TagName;
    }
    else
    {
        selectedTagName = cmbTagNames.Text ?? string.Empty;
    }
}
