Dim codes As New HashSet(Of String)(
    If(codesCsv, String.Empty).
        Split(New Char() {","c}, StringSplitOptions.RemoveEmptyEntries).
        Select(Function(s) s.Trim()),
    StringComparer.OrdinalIgnoreCase)
